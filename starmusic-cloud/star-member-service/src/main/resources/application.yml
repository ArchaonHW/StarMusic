server:
  port: 8086

spring:
  application:
    name: star-member-service
  datasource:
    url: jdbc:h2:file:./data/memberdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ${STARMUSIC_DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: false

starmusic:
  jwt-secret: ${STARMUSIC_JWT_SECRET:starmusic-dev-secret-do-not-use-in-prod-0123456789}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
